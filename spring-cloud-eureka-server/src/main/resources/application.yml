server:
  port: 8081
  servlet:
    context-path: /eureka-server
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url: http://${eureka.instance.hostname}:${server.port}/${server.servlet.context-path}