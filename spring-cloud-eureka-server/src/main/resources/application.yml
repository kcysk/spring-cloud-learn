server:
  port: 8091
  servlet:
    context-path: /eureka-server
eureka:
  instance:
    hostname: localhost
  client:
    #独立模式开启
    #register-with-eureka: false
    #fetch-registry: false
    service-url:
       defaultZone: http://localhost:8094/eureka-server-2/eureka/